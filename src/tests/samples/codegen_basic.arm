package arimo.codegenbasic;

public class Application {
    public static main() : Void {
        Integer a = 10;
        Integer b = 3;
        Integer sum  = a + b;
        Integer diff = a - b;
        Integer prod = a * b;
        Integer quot = a / b;

        IO.print("a + b = 13? test");

        if (a > b) {
            IO.print("a buyuk");
        } else {
            IO.print("b buyuk");
        }

        Integer count = 3;
        while (count > 0) {
            IO.print("sayiyor...");
            count = count - 1;
        }

        for (Integer i = 0; i < 3; i++) {
            IO.print("for dongusu");
        }

        IO.print("codegen_basic: OK");
    }
}
